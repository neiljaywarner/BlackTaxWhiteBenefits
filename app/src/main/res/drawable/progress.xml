<?xml version="1.0" encoding="utf-8"?>
<rotate xmlns:android="http://schemas.android.com/apk/res/android"
        android:pivotX="50%" android:pivotY="50%" android:fromDegrees="0"
        android:toDegrees="360">
    <shape android:shape="ring" android:innerRadiusRatio="3"
           android:thicknessRatio="6" android:useLevel="false">

        <size android:width="@dimen/progressBarSize" android:height="@dimen/progressBarSize" />
        <gradient android:type="sweep" android:useLevel="false"
                  android:startColor="@color/colorSecondaryLight"
                  android:endColor="#00ffffff"
                  android:angle="0"
        />
    </shape>
</rotate>
<!-- TODO: You could a) work at this really hard b) Use lottiefiles.com or c) get help from an artist or d) delete it
because the default of no drawable in the layout file progressbar control actually isn't too bad-

options include
https://www.lottiefiles.com/684-progress-loading (carina likes)
-->